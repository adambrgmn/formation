######################
# Install ellipsis
######################
if [[ $(cli_is_installed 'ellipsis') == 0 ]]; then
  echo_install "Installing ellipsis"
  curl ellipsis.sh | PACKAGES='adambrgmn/dotfiles' sh
  print_in_green "${bold}✓ installed!${normal}\n"
else
  print_success_muted "ellipsis already installed. Updating instead."
  ellipsis pull
fi
